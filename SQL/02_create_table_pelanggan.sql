CREATE TABLE transaksi_awal (
    no_order VARCHAR(10),
    tanggal DATE,
    nama_pelanggan VARCHAR(100),
    nama_barang VARCHAR(100),
    harga DECIMAL(10,2),
    jumlah INT,
    total DECIMAL(12,2)
);
